<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <!-- Customize color of navigation drawer icon and back arrow -->
        <item name="colorControlNormal">@color/colorPrimaryDark</item>
    </style>

    <style name="LinkeTextInDetailStyle">
        <item name="android:padding">8dp</item>
        <item name="android:textSize">@dimen/text_size_detail_links</item>
        <item name="android:textColor">@color/colorPrimaryDark</item>
    </style>

    <style name="DescriptionTextStyle">
        <item name="android:textSize">@dimen/text_size_description_detail</item>
    </style>

    <style name="fabStyleBotton">
        <item name="android:layout_alignParentEnd">true</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:layout_marginStart">@dimen/fab_margin</item>
        <item name="android:layout_marginTop">@dimen/fab_margin</item>
        <item name="android:layout_marginEnd">@dimen/fab_margin</item>
        <item name="android:layout_marginBottom">@dimen/fab_margin</item>
        <item name="android:clickable">true</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:foregroundTint">@color/colorPrimary</item>
        <item name="android:foregroundTintMode">add</item>
        <item name="android:hapticFeedbackEnabled">true</item>
        <item name="android:tint">@color/colorPrimary</item>
    </style>

    <style name="buttons_in_machine_detail">
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginEnd">8dp</item>
    </style>

    <style name="labels_eff_calc">
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:layout_marginRight">8dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:textAlignment">viewEnd</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="enter_text_style">
        <item name="android:layout_marginEnd">8dp</item>
    </style>

    <style name="divider_style">
        <item name="android:layout_margin">8dp</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="layout_efficiency_calc">
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

    <style name="app_bar_image_style">
        <item name="android:foregroundTint">@color/colorAccent</item>
        <item name="android:foregroundTintMode">screen</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <style name="CollapsingToolbarLayout_style">
        <item name="android:foregroundTint">@color/colorAccent</item>
        <item name="android:foregroundTintMode">screen</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

    <style name="labels_eff_calc_main_variable_style" parent="labels_eff_calc">
        <item name="android:layout_margin">16dp</item>
    </style>

    <style name="labels_eff_calc_item" parent="labels_eff_calc">
        <item name="android:textColor">@color/colorWhite</item>
    </style>
    

    <style name="Progress_Bar_Theme" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="minHeight">16dip</item>
        <item name="maxHeight">30dip</item>
    </style>

</resources>
